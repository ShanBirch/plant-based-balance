# Join Balance sales entry and completed-run recovery

The login page's ordinary Join Balance/email signup redirected to a bare dashboard URL. It did not activate the account-first sales flow used by Meta and website leads. Recent completed-tour activity confirmed client_activation mode with all 20 steps completed; the latest account was created just before the previous payment-handoff deployment, so an already-running page could retain the old Course destination.

Direct email signup now creates account-first sales entry parameters, activates an account-owned preview after successful account creation, and carries the entry through the dashboard redirect and confirmation-email return URL. The direct source is app_signup; no Facebook/paid-social attribution is fabricated. A claimed or differently owned preview on the same phone cannot supply the new account's state. Pending paid purchase handoffs keep their existing route and their parameters are captured before claims clear session receipts.

Recent accounts that already completed the old tour now check their account status during startup and recover the checkout checkpoint without repeating setup, lessons, or the coach video. The two recent completed-run records were checked read-only and have durable tour-completion timestamps. Existing paid members are exempted from automatic first-run tours after their profile loads. Settings replay explicitly opts into the ordinary app tour; it does not activate the sales flow.

Validation: 28 focused tests passed for entry, existing-account exemptions, gate enforcement, payment routing, new-account ownership, paid handoffs, explicit Settings replay and completed-run recovery. Browser checks click the actual Join Balance link, complete the signup form with stubbed auth, carry real preview state to the dashboard, exercise tour completion/payment and reload persistence on both clean and previously used phones. A separate check invokes the full checkAndTriggerOnboarding function for a completed old signup and confirms payment opens without rerunning setup or switching to Course. Signup mobile checks passed 12 viewport/theme/inset combinations. Browser tests intercept external requests; no live accounts, payments or subscriptions were created or modified.
