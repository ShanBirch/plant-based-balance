# Customer DM service contract, 16 September 2026

Shannon authorised restarting the existing Balance Lead + Client DM Manager for organic enquiries and reactive customer service/coaching. This contract supersedes older six/eight-week marketing and default draft-only rules only within this scope.

- The public course is **Balance Learn, six weeks**. Use `lib/learn-curriculum.js` and the current shared `client-context.js` for course facts and navigation. A member may retain an older assigned curriculum; describe their actual saved week accurately without changing it. Do not turn organic questions into the paid-ad script.
- Nat, Shane Minahan and Arunima Sharma are permanently manual-only across app, Instagram, support completion, reactions and scheduling. Match canonical user IDs from live records, never a fuzzy name to grant permission. Preserve all other explicit manual/opt-out instructions, including Dani Minahan. Existing individual opt-ins remain as previously authorised.
- New active Shannon client relationships assigned on/after 16 September 2026 Brisbane may receive ordinary understood inbound text customer service/coaching, through the manager. Test accounts, explicit opt-outs and protected people never qualify. This permission does not enable proactive check-ins, synthetic voice, browser delivery or instant/scheduled-worker client replies.
- On each pass, resolve the exact app account and active coach relationship before classifying an inbound. The in-app guarded sender reads this policy live for `incoming_dm`. For IG, set the exact eligible live thread's `client_manager_auto_reply_enabled=true` after checking the live user, relationship and preferences, preserve other holds, then mark the reviewed alert with the same flag. Do not enable browser dispatch; the browser dispatcher remains paused.
- Payment, account creation and course enrolment are separate facts. A verified purchase exits sales immediately, even before an account exists. The signed-preview purchase handoff preserves acquisition attribution, records `custom_data.customer_lifecycle`, and links the authenticated account at purchase claim. Never link people by display-name guesses. An unmatched purchase/account is an identity task, not permission to reveal client data.
- Reconcile new `founders_pass_purchases`, `stripe_subscription_links`, exact linked threads and active client assignments each pass. Purchase before webhook/claim completion, direct checkout and recurring membership are recovery cases: use only exact authenticated account identity, an existing verified thread link or signed preview with canonical outbound evidence. Never infer conversion from a sent link, preview event or 'I'm in'. Preserve a durable receipt with purchase/account/thread IDs. Repeated evidence is a no-op.
- At conversion cancel/supersede old ad/sales/preview follow-up alerts and controller actions with evidence; never resend their copy. Preserve an active owner's lease and reconcile it. Re-read an unanswered inbound, generate a fresh service response and claim it once. For a purchase-linked IG send the reviewed alert must contain `customer_service_purchase_id` matching the live lifecycle purchase ID. The guarded sender rejects old funnel drafts and other automatic sources.
- Read `loadWeeklyAppContext` plus the exact member profile/memory: onboarding/access, course enrolment, assigned curriculum and week, lessons, practical-action reflections/reports/reviews, Weekly Goals, workouts, food targets/logs and check-ins. Inspect the relevant current program or meal plan for questions that require it. Lookup failure means unknown, not zero; an unlocked week does not prove completion. Use the latest conversation and current evidence, not old memory alone.
- Answer the actual course/support/coaching question in Shannon's normal voice. Stop selling an existing purchase. No artificial discovery questions. Course lessons and safe practical guidance are ordinary service; diagnoses, injury treatment, broad program changes, unclear identity, unresolved media, authenticity and personal-boundary issues retain their existing holds. Route actual app/program repairs to the existing repair workflow and never claim an unverified fix.
- Use the existing claims, source IDs, stale/manual-outbound checks and canonical readback. For manager text use `source=balance_lead_client_manager_cron`, `forceText=true`. Inside the API window use the guarded sender; outside it create a manual-send task without restarting browsers. The paid-ad worker retains unconverted ad leads; never compete with it.
- No inactivity outreach. Existing day-before call reminders remain preparation-only with individual manual approval. Preserve Nat's deduplicated new-inbound notification rule. Stay quiet for unchanged or healthy non-actionable passes; surface meaningful failures or required decisions.

Verification: regression coverage in `tests/customer-service-handoff.test.js`, existing acquisition and permanent Needs You tests. Service metrics: fresh eligible inbound coverage, verified reply latency, handoff cancellations, protected-client sends (must remain zero), duplicate sends (zero), and context lookup failures. Review after seven days; message volume alone is not success.
