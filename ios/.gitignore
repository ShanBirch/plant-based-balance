App/build
App/Pods
App/output
App/App/public
DerivedData
xcuserdata

# Cordova plugins for Capacitor
capacitor-cordova-ios-plugins

# Generated Config files
# Note: capacitor.config.json is committed here because we don't run
# `npx cap sync` as part of the Xcode build. It must be present for
# Capacitor to read appendUserAgent and server URL settings.
App/App/config.xml
